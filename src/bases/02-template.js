
const username = 'Erick';
const lastnanme = 'Ruiz';

const fullname = `${username} ${lastnanme}`

console.log(fullname);
console.log( sayHi(username) );

function sayHi(username) {
    return `Hi ${username}`
}

